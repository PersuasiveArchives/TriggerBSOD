This triggers the bsod, do this on a vm, if you want in a physical machine do it by your risk!
To compile type this in the terminal
```
gcc -o trigger.c output.exe -lgdi32 -luser32
```
This works on Windows XP
